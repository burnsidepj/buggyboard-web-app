# Feature specs

One Markdown file per feature (e.g. `01-login.md`, `02-board.md`). Each spec includes user stories, Gherkin scenarios, and acceptance criteria. Created when you add a feature. When the feature is implemented, Playwright tests **must** cover those scenarios (see `specs/engineering/test-automation-patterns.md`).
